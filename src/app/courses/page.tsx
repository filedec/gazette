import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Explore Courses',
};

const Page = () => {
  return <div>Page</div>;
};

export default Page;
